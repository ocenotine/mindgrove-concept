
import { useToast, toast } from "@/components/ui/use-toast";

// This is a wrapper around the toast component from shadcn/ui
// Re-exporting it to make it available throughout the app
export { useToast, toast };
