
// This component is intentionally left empty as we're removing the OpenRouter API configuration 
// section from student settings as requested

// We'll return null so it doesn't render anything
import React from 'react';

const ApiKeySettings: React.FC = () => {
  return null;
};

export default ApiKeySettings;
